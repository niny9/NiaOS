# Invest OS 依赖包

# ============ 核心数据源（完全免费，无需token） ============
# Baostock - 历史数据最全面（强烈推荐！）
baostock>=0.8.9

# AKShare - 实时和历史数据
akshare>=1.12.0

# Mootdx - 通达信数据
mootdx>=0.8.0

# easyquotation - 实时行情（新浪/腾讯）
easyquotation>=0.1.1

# Ashare - 双核心自动切换
Ashare>=1.0.0

# ============ 新增：PDF解析 ============
pdfplumber>=0.9.0
PyPDF2>=3.0.0

# ============ 新增：量化框架 ============
# vectorbt - 快速矢量化回测（可选，安装较慢）
# vectorbt>=0.25.0

# FinRL - 强化学习交易（可选，依赖复杂）
# finrl>=0.3.6
# stable-baselines3>=2.0.0
# gymnasium>=0.28.0

# ============ 新增：机器学习 ============
scikit-learn>=1.3.0
xgboost>=2.0.0

# ============ 数据处理 ============
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# ============ 数据库 ============
sqlalchemy>=2.0.0

# ============ HTTP请求 ============
requests>=2.31.0
httpx>=0.24.0

# ============ 日志和工具 ============
python-dotenv>=1.0.0
tqdm>=4.65.0

# ============ 类型检查（开发用） ============
# mypy>=1.5.0
# pandas-stubs>=2.0.0
# types-requests>=2.31.0

# ============ 注意事项 ============
# 1. vectorbt和finrl是可选依赖，安装时间较长
# 2. 如需使用，请单独安装：
#    pip install vectorbt
#    pip install finrl
# 3. 核心功能不依赖这两个包
